import React from "react";
import "./ClosingButton.scss";

const ClosingButton = (props) => (
  <div className="ClosingButton" {...props}>
    <div className="cross-1">
      <div className="cross-2" />
    </div>
  </div>
);

export default ClosingButton;
